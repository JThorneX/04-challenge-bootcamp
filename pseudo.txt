button to start - eventlistener

timer starts -- counts down normally unless answer wrong than subtract an extra 5 seconds  (difficulties ? 2s 8s?) if else statements

display wrong somewhere without impeding the quiz? a red -5 by the timer?

questions must store the correct answer -- array since multiple choice?
/container

new question when answered old

timer reaches 0 === game over

scoreboard with place for initials
turn initials + score into array and display with array?

scoring system (just use the seconds?)


split up questions and answers into two divs -- use arrays probably